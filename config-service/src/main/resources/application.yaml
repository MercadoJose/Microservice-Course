server:
  port: 8084
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/MercadoJose/Microservice-Course
          search-paths: config-data
  application:
    name: config-service
